import { createContext } from "react";

export const MyContext  = createContext("");    

/* it is used as a container which shares the data to its child components without giving props to  every component */